server:
  port: 8761
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SpirngCloudServer/resources
          search-paths: respo
          username: zhangdianyang
          password: zdy1991227
      label: master
