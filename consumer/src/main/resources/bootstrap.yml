spring:
  cloud:
   config:
    uri: http://localhost:8762/
    label: master
    profile: dev
    name: "${spring.application.name},application}"
  application:
    name: consumer
management:
  security:
    enabled: false



